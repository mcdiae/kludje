uploadArchives {
  repositories {
    mavenDeployer {
      pom.project {
        name 'kludje-annotation'
        packaging 'jar'
        description 'Java 8 lambda API annotations'
      }
    }
  }
}