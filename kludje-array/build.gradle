uploadArchives {
    repositories {
        mavenDeployer {
            pom.project {
                name 'kludje-array'
                packaging 'jar'
                description 'Java 8 array API'
            }
        }
    }
}