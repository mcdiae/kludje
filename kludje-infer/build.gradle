uploadArchives {
    repositories {
        mavenDeployer {
            pom.project {
                name 'kludje-infer'
                packaging 'jar'
                description 'Java 8 lambda meta API'
            }
        }
    }
}